/*
CHALLENGE 1
add function
w args
a and b
return sum

import to server/main
print result of the two numbers
10 + 12
*/
/*
export let add = function (a, b) {
  return a + b;
};
*/

/*
CHALLENGE 2

have a default export that is a function that adds the two vals together
tweak server.js to load in the value as a default

*/
/*
let addtwo = function (a, b) {
  return a + b;
};

export default addtwo;
*/
// or can just do:
//export default function (a, b) { return a + b; };
